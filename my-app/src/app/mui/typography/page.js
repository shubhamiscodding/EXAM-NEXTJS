'use client';

import Typography from '@mui/material/Typography';

export default function MUITypographyPage() {
  return (
    <div>
      <h1>/mui/typography</h1>
      <Typography variant="h2" gutterBottom>
        Material UI Typography
      </Typography>
      <Typography variant="body1">
        This is an example of the Material UI Typography component rendered in the /mui/typography route.
      </Typography>
    </div>
  );
}